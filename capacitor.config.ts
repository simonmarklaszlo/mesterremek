import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.leaflettest',
  appName: 'leaflettest',
  // Angular build outputs directly to www per angular.json
  webDir: 'www'
};

export default config;
